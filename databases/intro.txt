show databases;

create database cars_db;
-- i++;

use cars_db;

create table cars(
  id int auto_increment,
  title text not null ,
  make_id int not null,
  year int not null,
  primary key(id)
);

create table makes(
  id int auto_increment,
  title text not null ,
  primary key(id)
);

insert into cars(title,make_id,year) values('Mazda Atenza',1,2013);

insert into cars(title,make_id,year) values('Axela',1,2015);

insert into cars(title,make_id,year) values('Jeep Cherokee',2,2015);


insert into makes(title)values('Mazda');a

insert into makes(title)values('Jeep');
-- 
select * from cars;
-- make_id
select * from makes;

select cars.*,makes.title make from cars join makes on makes.id=cars.make_id